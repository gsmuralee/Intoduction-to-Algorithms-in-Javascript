// Insertion Sort
let array = [5,2,4,3,1,6];
for(let j = 1; j <= array.length; j++){
    let k = array[j],i = j-1;
    while(i >= 0 && array[i] > k){
        array[i+1] = array[i];
        array[i] = k;
        i--;
    }
}
